// Seed canary dataset (docs only; tests use in-memory mocks)
// - 1 child, 30 days window, 12 events with 2 types.
// This script is a placeholder to document the dataset shape.

console.log('Use tests under __tests__/e2e/canary.v2.test.ts as reference for seeding dataset in CI environment with a real DB.')

